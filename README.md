# Tolerance GUI

Wizard-style GUI to batch‑update tolerances in Seica XML component files.

## Quick start

```bash
python -m venv .venv && source .venv/bin/activate   # Windows: .\.venv\Scripts\activate
pip install -r requirements.txt
python tolerance_gui.py
```
# xml_editor
